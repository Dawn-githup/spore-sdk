name: Vitest Test

on:
  push:
    branches:
      - main
  workflow_dispatch: {}
  schedule:
    - cron: '0 22 * * *' # 每天早上6点触发 (UTC时间)

jobs:
  test:
    runs-on: ubuntu-latest

    env:
      SHELL: /bin/bash

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install dependencies
        run: |
          curl -fsSL https://get.pnpm.io/install.sh | sh
          export PATH="/usr/local/bin:$PATH" # 修改为您的 pnpm 安装路径
          pnpm install

      - name: Run Vitest tests
        run: npm test
